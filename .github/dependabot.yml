# Keeps the supply chain current despite SHA-pinning:
# Dependabot bumps the pinned action SHAs (and the trailing version comment)
# and the Go/Bun dependencies on a weekly cadence.
version: 2
updates:
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    commit-message:
      prefix: ci

  - package-ecosystem: gomod
    directory: /
    schedule:
      interval: weekly
    commit-message:
      prefix: build

  - package-ecosystem: bun
    directory: /frontend
    schedule:
      interval: weekly
    commit-message:
      prefix: build
