# CLAUDE.md

Read @AGENTS.md — it is the single source of truth for this repository.

Nested `AGENTS.md` files add scoped rules for the area you are editing
(`internal/`, `frontend/`, `frontend/src/iframe/`). The reasoning behind
individual decisions lives in comments next to the code it explains.

Do not add project rules to this file — they belong in `AGENTS.md` so every tool
reads the same instructions.
